### [SH-123] Especificación: Gapless TTS Playback (Web Audio API)

#### 1. Visión General (Abstract)
El BFF transfiere los eventos `SirenEvent` hacia la UI. Cuando estos eventos incluyen el campo `tts_audio_chunk` (codificado en Base64 por la serialización JSON del Protobuf) y un `sample_rate`, el frontend debe reconstruir el audio y reproducirlo de forma fluida. Se implementará un `TTSPlayer` utilizando la `Web Audio API` para encolar los buffers secuencialmente y garantizar una reproducción "Gapless" (sin interrupciones).

#### 2. Arquitectura de Sincronización de Audio
No podemos usar etiquetas `<audio>` estándar. El `TTSPlayer` debe:
1. Instanciar un `AudioContext` global (o reutilizar el de la captura de micrófono).
2. Mantener un puntero de tiempo matemático (`nextStartTime`).
3. Al recibir un chunk Base64 (PCM Int16):
   - Decodificar Base64 a `Uint8Array` -> `Int16Array`.
   - Convertir de `Int16` a `Float32` (El formato nativo que demanda Web Audio API).
   - Crear un `AudioBuffer` de 1 canal con el `sample_rate` del evento.
   - Instanciar un `AudioBufferSourceNode`, asignarle el buffer y programar su inicio: `source.start(nextStartTime)`.
   - Actualizar el puntero: `nextStartTime += audioBuffer.duration`.

#### 3. Control de Jitter y Resiliencia
- **Resync del Reloj:** Si el Kernel tarda en enviar el siguiente chunk y `nextStartTime` cae en el pasado (respecto a `audioContext.currentTime`), el puntero debe resetearse a `audioContext.currentTime + 0.05` (añadiendo un micro-buffer de 50ms para evitar chasquidos).
- **Gestión de Memoria:** Los nodos reproducidos deben ser eliminados de la memoria (`onended` event) para prevenir *memory leaks* en conversaciones largas.

#### 4. Criterios de Aceptación (DoD)
1. Clase/Módulo utilitario `TTSPlayer.ts` creado de forma independiente (desacoplado de la UI).
2. `useAegisStore.ts` intercepta los eventos de Siren con `tts_audio_chunk` y los deriva al `TTSPlayer`.
3. La decodificación de Base64 a PCM Float32 es matemáticamente precisa y no bloquea el hilo principal de React.
4. El audio se reproduce fluidamente, formando frases coherentes sin cortes abruptos entre chunks.